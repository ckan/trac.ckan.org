id,summary,reporter,owner,description,type,status,priority,milestone,component,resolution,keywords,cc,repo,theme
1729,Helper function for extensions to add vocabularies to a ckan instance,seanh,johnglover,"Add a helper function to make it easy for extensions to add new vocabularies to default_package_schema(). The helper function should take the name or ID of a vocabulary (which should already exist in the db) and add the necessary key: [schema] entry to default_package_schema(), with the necessary validation, authorisation, transformation.",enhancement,closed,major,ckan-sprint-2012-02-20,ecportal,duplicate,taxonomies,,ckan,none
