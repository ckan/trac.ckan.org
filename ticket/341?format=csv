id,summary,reporter,owner,description,type,status,priority,milestone,component,resolution,keywords,cc,repo,theme
341,Web UI accepts package IDs in URLs,dread,,"(in the same way that we can refer to packages in the API by ID as well as name.)

== As a ==

client of CKAN

== I want to ==

link to a package page in the UI, referring to it by ID",enhancement,closed,awaiting triage,,ckan,fixed,,,,
